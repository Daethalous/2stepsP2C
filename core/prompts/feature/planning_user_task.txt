Your goal is break down implementation tasks according to PRD/technical design, generate a stable task list, and analyze task dependencies.
You will break down tasks for adapting the baseline repository to the paper, but this stage is ONLY for task decomposition and per-file logic summaries. Do not output closure contracts, public interface migration tables, or anti-simplification rules in this stage.

The overall plan is:
$overview

The architecture design is:
$design

The baseline repository map is:
$repo_manifest_summary

Resolved entrypoint candidates and execution hints are:
$entrypoint_summary

Baseline RPG interface stub summary:
$baseline_interface_stub

Now, generate a concise `Task list` and `Logic Analysis` for the files that must be touched.
The goal of this stage is to stabilize the file set and dependency order before later stages fill in integration contracts.
For every modified file, `Logic Analysis` should summarize the main symbols or responsibilities, the important upstream/downstream dependencies, and the synchronized edits that later stages must preserve.
Do NOT output `Modification Closure`, `Callsite Update List`, `Public Interface Changes`, `Anti-Simplification Constraints`, or `Forbidden File Names` here.
Never solve the task by creating numbered Python files or shadow modules that bypass the main execution path.
Every `Task list` item must be a pure relative file path string only, such as `main.py`, `detectors/logprob_detector.py`, `notes/ablation.md`, or `reports/error_cases.txt`.
Do NOT use directory-only entries, absolute paths, Windows drive paths, `..`, glob patterns, or any explanatory suffix such as `: implement ...`, `, update ...`, or prose after the path.
If a description is needed, put it in `Logic Analysis`, not in `Task list`.
Every `Logic Analysis` item must be exactly `[relative_file_path, description]`.
The first element of each `Logic Analysis` item must be a single pure relative file path only. Do NOT merge multiple files into one key such as `run.sh and requirements.txt`.
If two files need analysis, split them into two separate items, for example `["run.sh", "..."]` and `["requirements.txt", "..."]`.

-----

## Format Example
[CONTENT]
{
    "Required packages": [
        "numpy==1.21.0",
        "torch==1.9.0"
    ],
    "Required Other language third-party packages": [
        "No third-party dependencies required"
    ],
    "Logic Analysis": [
        [
            "dataset_loader.py",
            "Symbols: build_dataset, create_dataloader. Upstream callers: main.py and trainer.py setup path. Downstream dependencies: model.py and evaluation.py consume the produced sample schema. Synchronized edits: update config parsing, imports, and batch-field names."
        ],
        [
            "model.py",
            "Symbols: Model.forward, Model.__init__. Upstream callers: trainer.py and evaluation.py. Downstream dependencies: helper layers and output schema consumers. Synchronized edits: update constructor args, return-value handling, and imports."
        ],
        [
            "trainer.py",
            "Symbols: Trainer.train, Trainer.compute_loss. Upstream callers: main.py. Downstream dependencies: evaluator.py consumes prediction schema. Synchronized edits: update constructor arguments, return-value handling, and any registry or factory wiring."
        ],
        [
            "evaluation.py",
            "Symbols: run_eval, evaluate_batch. Upstream callers: main.py or scripts. Downstream dependencies: reporting utilities and saved metrics schema. Synchronized edits: update model invocation, output unpacking, and metrics handling."
        ],
        [
            "main.py",
            "Symbols: main, build_from_config. Upstream callers: CLI entrypoint. Downstream dependencies: trainer.py, dataset_loader.py, and evaluation.py. Synchronized edits: update imports, config wiring, factory selection, and execution chain."
        ]
    ],
    "Task list": [
        "dataset_loader.py",
        "model.py",
        "trainer.py",
        "evaluation.py",
        "main.py"
    ],
    "Shared Knowledge": "Both data_preprocessing.py and trainer.py share common config variables and utility helpers.",
    "Anything UNCLEAR": "Clarification needed on recommended hardware configuration for large-scale experiments."
}
[/CONTENT]

## Nodes: "<node>: <type>  # <instruction>"
- Required packages: typing.Optional[typing.List[str]]  # Provide required third-party packages in requirements.txt format.
- Required Other language third-party packages: typing.List[str]  # List packages required for non-Python languages. If none, specify "No third-party dependencies required".
- Logic Analysis: typing.List[typing.List[str]]  # Provide a list of `[relative_file_path, description]` items. The first element must be a single file path only. For modified files, explicitly include replaced symbols or responsibilities, upstream callers, downstream dependencies, and synchronized edits at a concise level. Keep each description as 1-3 plain-text sentences only. Do not embed code blocks, malformed quotes, or complex escaping inside JSON strings.
- Task list: typing.List[str]  # Break down the tasks into a list of relative file paths, prioritized based on dependency order. Each item must be a pure file path only, without descriptions. The task list must contain the same file set as `Logic Analysis`.
- Shared Knowledge: <class 'str'>  # Detail any shared knowledge, like common utility functions or configuration variables.
- Anything UNCLEAR: <class 'str'>  # Mention any unresolved questions or clarifications needed from the paper or project scope.

## Constraint
Format: output wrapped inside [CONTENT][/CONTENT] like the format example, nothing else.
- Prefer updating the existing main/train/eval/factory/registry path over introducing bypass code.
- Do not propose numbered filenames or numbered imports.
- `Task list` items must be real relative file paths only, not task titles or directory names. Do not restrict yourself to only Python/YAML/JSON files when the task genuinely targets another file type.
- `Logic Analysis` file keys must also be real relative file paths only, one file per item, not merged labels or descriptions.
- `Task list` and `Logic Analysis` must contain the same file set.
- Keep descriptions concise plain text. Do not place code blocks or heavily escaped pseudo-code inside JSON strings.

## Action
Follow the node instructions above, generate your output accordingly, and ensure it follows the given format example.