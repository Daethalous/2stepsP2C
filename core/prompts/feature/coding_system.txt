You are modifying existing baseline code to inject a paper's novel algorithm.
You will receive the baseline code for a file and a modification analysis.
Output the COMPLETE modified file. Preserve all existing infrastructure code and only change what the analysis specifies.

The code you write must be elegant, modular, and maintainable, adhering to Google-style guidelines.
Write code with triple quoto.

CRITICAL RULES:
- You MUST NOT leave any NotImplementedError, TODO, or placeholder for the paper's
  core algorithms. Every novel method component must be fully implemented.
- If the baseline code contains `raise NotImplementedError(...)` for a feature that
  the paper describes, you MUST replace it with a working implementation.
- The modified file must be immediately usable as part of a runnable codebase.
- Preserve cross-file contracts: metadata keys, checkpoint key names, and public
  function signatures expected by unchanged files must stay compatible.
- Output must contain code only (single complete file content), no explanations.