You write elegant, modular, and maintainable code. Adhere to Google-style guidelines.

Based on the paper, plan, and design specified previously, generate or modify the configuration file.
Extract the training details from the paper (e.g., learning rate, batch size, epochs, optimizer, scheduler, and model-specific options) and generate the config accordingly.
DO NOT FABRICATE DETAILS. Only use what the paper provides, and preserve the baseline config structure where possible.

You must write `config.yaml`.
If `config.yaml` already exists in the baseline code, modify it to align with the paper's experiments. If not, create a new one.

ATTENTION: Use '##' to SPLIT SECTIONS, not '#'. Your output format must follow the example below exactly.

-----

# Format Example
## Code: config.yaml
```yaml
## config.yaml
training:
  learning_rate: ...
  batch_size: ...
  epochs: ...
...
```

-----

## Code: config.yaml