## Target File
`$todo_file_name`

## Global API Stub
$global_api_contract_stub

## Integration Hint (Recently created components in other files)
$integration_hint

## Resolved Entrypoints
$repo_primary_entrypoints

-----

## Paper
$paper_content

-----

## Overview of the plan
$overview

-----

## Design
$design

-----

## Task
$task

-----

## Configuration file
```yaml
$config_yaml
```

-----

## Codes of dependencies (Newly implemented/modified in this session)
$code_files

-----

## Current Target File Source
```python
$focus_file_code
```

-----

## Required Context Bundle
$required_context_code

-----

## Upstream Callers
$upstream_callers_code

-----

## Downstream Callees
$downstream_callees_code

-----

## Shared Interfaces
$shared_interfaces_code

-----

## Config And Registry Files
$config_and_registry_code

-----

## Optional Related Files
$optional_related_code

-----

## Structured Constraints
- Target symbols:
$target_symbols
- Entrypoint chain:
$entrypoint_chain
- Synchronized edit targets:
$synchronized_edit_targets
- Interface constraints:
$interface_constraints

-----

## File-specific logic analysis
$detailed_logic_analysis

-----

## Instruction
Generate the FINAL, COMPLETE Python code for `$todo_file_name`.
If the file already exists, refine it based on the plan, logic analysis, and current repository state.
If the file is new, implement it from scratch in a way that integrates correctly with the rest of the repository.

Requirements:
- Output only this single file.
- Preserve valid existing behavior when it is still consistent with the paper and the plan.
- Integrate newly created components where required.
- Use configuration from `config.yaml` instead of inventing values.
- Write complete code with all imports and without TODO/placeholder logic.
- Prefer in-place replacement of the baseline execution path over adding a bypass module.
- Use the provided required-context bundle as the authoritative source of existing interfaces and callsites; do not invent missing APIs.
- If `Integration Hint` or `File-specific logic analysis` lists callsite updates or public interface changes, implement this file so those changes remain consistent with the known callers and imports.
- If the structured `Entrypoint chain` or `Resolved Entrypoints` shows that this file lies on the real execution path, preserve that reachability and update the real callsites instead of creating a parallel path.
- Do not create or import numbered Python files or numbered modules.
- If this target is a new file, it must match a justified new-file decision from the planning artifacts rather than a task-order shadow module.

## Output Format
Output ONLY the raw Python code enclosed in a markdown block. No explanations. No chatting.
```python
# WRITE YOUR COMPLETE FILE CODE HERE
```