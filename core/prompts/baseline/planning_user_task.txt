Your goal is to break down tasks according to the architectural design, generate a task list, and analyze task dependencies for the EXPERIMENTAL INFRASTRUCTURE AND BASELINE ONLY. Do NOT attempt to implement the paper's novel algorithm.
             
Based on the architecture design for building the foundational experimental infrastructure, let's break down tasks, generate a task list, and analyze task dependencies.
The Logic Analysis should not only consider the dependencies between files but also provide detailed descriptions to assist in writing the baseline code. YOU MUST explicitly mention the exact Tensor shapes and the stable external interface required for the vanilla baseline model in your analysis.

-----

## Format Example
[CONTENT]
{
    "Required packages": [
        "numpy==1.21.0",
        "torch==2.0.0",
        "pandas"
    ],
    "Required Other language third-party packages": [
        "No third-party dependencies required"
    ],
    "Logic Analysis": [
        [
            "dataset_loader.py",
            "Handles loading and preprocessing of datasets. Prepares sliding windows and returns tensors with exact shapes (e.g., features [B, L, N])."
        ],
        [
            "model.py",
            "Defines the baseline model (e.g., a vanilla ResNet, Transformer, linear model, or other standard architecture compatible with the paper's setup). MUST explicitly define the forward pass signature as forward(x: Tensor[B, L, N]) -> Tensor[B, S, N] or the exact equivalent required by the task, and preserve a stable external interface for later feature-stage replacement."
        ],
        [
            "trainer.py",
            "Trainer class implementing the training loop, loss computation (MSE/MAE), and optimization. Depends on dataset_loader.py and model.py."
        ],
        [
            "evaluation.py",
            "Evaluation class calculating metrics on the original scale. Depends on dataset_loader.py and model.py."
        ],
        [
            "main.py",
            "Entry point orchestrating data loading, model initialization, training, and evaluation."
        ]
    ],
    "Task list": [
        "dataset_loader.py", 
        "model.py",  
        "trainer.py", 
        "evaluation.py",
        "main.py" 
    ],
    "Full API spec": "",
    "Shared Knowledge": "Common configurations such as default lookback window L, prediction horizon S, and batch size.",
    "Anything UNCLEAR": "Need clarification on the exact random seed or hardware device preferences."
}
[/CONTENT]

## Nodes: "<node>: <type>  # <instruction>"
- Required packages: typing.Optional[typing.List[str]]  # Provide required third-party packages in requirements.txt format.(e.g., 'numpy==1.21.0').
- Required Other language third-party packages: typing.List[str]  # List down packages required for non-Python languages. If none, specify "No third-party dependencies required".
- Logic Analysis: typing.List[typing.List[str]]  # Provide a list of files with the classes/methods/functions to be implemented, including dependency analysis and imports. You MUST enforce the Tensor shape constraints and the stable external interface for the baseline model. Include as much detailed description as possible.
- Task list: typing.List[str]  # Break down the tasks into a list of filenames, prioritized based on dependency order (e.g., data and model first, then trainer, then main). The task list must include the previously generated file list.
- Full API spec: <class 'str'>  # Describe all APIs using OpenAPI 3.0 spec that may be used by both frontend and backend. If front-end and back-end communication is not required, leave it blank.
- Shared Knowledge: <class 'str'>  # Detail any shared knowledge, like common utility functions or configuration variables.
- Anything UNCLEAR: <class 'str'>  # Mention any unresolved questions or clarifications needed.

## Constraint
Format: output wrapped inside [CONTENT][/CONTENT] like the format example, nothing else.

## Action
Follow the node instructions above, generate your output accordingly, and ensure it follows the given format example.