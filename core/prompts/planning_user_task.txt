Your goal is break down tasks according to PRD/technical design, generate a task list, and analyze task dependencies. 
You will break down tasks, analyze dependencies.
             
You outline a clear PRD/technical design for reproducing the paper's method and experiments. 

Now, let's break down tasks according to PRD/technical design, generate a task list, and analyze task dependencies.
The Logic Analysis should not only consider the dependencies between files but also provide detailed descriptions to assist in writing the code needed to reproduce the paper.
All task-list filenames must remain descriptive and stable. Never introduce numbered Python files or task-order shadow modules such as `1_preprocess.py` or `3_model.py`.
Return strict JSON only. Keep all descriptive text inside valid JSON string values. Do not output trailing commas, dangling bullets, unmatched quotes, or malformed arrays before `Task list` / `Logic Analysis`.
Every `Task list` item must be a pure relative file path string only, such as `main.py`, `detectors/logprob_detector.py`, `notes/ablation.md`, or `reports/error_cases.txt`.
Do NOT use directory-only entries, absolute paths, Windows drive paths, `..`, glob patterns, or any explanatory suffix such as `: implement ...`, `, update ...`, or prose after the path.
If a description is needed, put it in `Logic Analysis`, not in `Task list`.
Every `Logic Analysis` item must be exactly `[relative_file_path, description]`.
The first element of each `Logic Analysis` item must be a single pure relative file path only. Do NOT merge multiple files into one key such as `run.sh and requirements.txt`.
If two files need analysis, split them into two separate items, for example `["run.sh", "..."]` and `["requirements.txt", "..."]`.

-----

## Format Example
[CONTENT]
{
    "Required packages": [
        "numpy==1.21.0",
        "torch==1.9.0"  
    ],
    "Required Other language third-party packages": [
        "No third-party dependencies required"
    ],
    "Logic Analysis": [
        [
            "data_preprocessing.py",
            "DataPreprocessing class ........"
        ],
        [
            "trainer.py",
            "Trainer ....... "
        ],
        [
            "dataset_loader.py",
            "Handles loading and ........"
        ],
        [
            "model.py",
            "Defines the model ......."
        ],
        [
            "evaluation.py",
            "Evaluation class ........ "
        ],
        [
            "main.py",
            "Entry point  ......."
        ]
    ],
    "Task list": [
        "data_preprocessing.py",
        "dataset_loader.py", 
        "model.py",  
        "trainer.py", 
        "evaluation.py",
        "main.py"  
    ],
    "Full API spec": "openapi: 3.0.0 ...",
    "Shared Knowledge": "Both data_preprocessing.py and trainer.py share ........",
    "Anything UNCLEAR": "Clarification needed on recommended hardware configuration for large-scale experiments."
}

[/CONTENT]

## Nodes: "<node>: <type>  # <instruction>"
- Required packages: typing.Optional[typing.List[str]]  # Provide required third-party packages in requirements.txt format.(e.g., 'numpy==1.21.0').
- Required Other language third-party packages: typing.List[str]  # List down packages required for non-Python languages. If none, specify "No third-party dependencies required".
- Logic Analysis: typing.List[typing.List[str]]  # Provide a list of `[relative_file_path, description]` items. The first element must be a single file path only, and the second element should contain the detailed dependency analysis and implementation notes. Keep each description as 1-3 plain-text sentences only. Do not embed code blocks, malformed quotes, or complex escaping inside JSON strings.
- Task list: typing.List[str]  # Break down the tasks into a list of relative file paths, prioritized based on dependency order. Each item must be a pure file path only, without descriptions. The task list must contain the same file set as `Logic Analysis`.
- Full API spec: <class 'str'>  # Describe all APIs using OpenAPI 3.0 spec that may be used by both frontend and backend. If front-end and back-end communication is not required, leave it blank.
- Shared Knowledge: <class 'str'>  # Detail any shared knowledge, like common utility functions or configuration variables.
- Anything UNCLEAR: <class 'str'>  # Mention any unresolved questions or clarifications needed from the paper or project scope.

## Constraint
Format: output wrapped inside [CONTENT][/CONTENT] like the format example, nothing else.
- Do not propose numbered Python filenames or numbered imports.
- The payload inside `[CONTENT]...[/CONTENT]` must be valid JSON parseable by `json.loads`.
- `Task list` items must be real relative file paths only, not task titles or directory names. Do not restrict yourself to only Python/YAML/JSON files when the task genuinely targets another file type.
- `Logic Analysis` file keys must also be real relative file paths only, one file per item, not merged labels or descriptions.
- `Task list` and `Logic Analysis` must contain the same file set.
- Keep descriptions concise plain text. Do not place code blocks or heavily escaped pseudo-code inside JSON strings.

## Action
Follow the node instructions above, generate your output accordingly, and ensure it follows the given format example.